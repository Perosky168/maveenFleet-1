{
  "name": "maveenfleet",
  "version": "1.0.0",
  "description": "up coming dev apps",
  "main": "server.js",
  "scripts": {
    "start-dev": "nodemon server.js",
    "migrate": "npx sequelize-cli db:migrate",
    "migrate:reset": "npx sequelize-cli db:migrate:undo:all && npm run migrate",
    "test": "jest",
    "pretest": "cross-env NODE_ENV=test npm run migrate:reset"
  },
  "jest": {
    "testEnvironment": "node",
    "coveragePathIgnorePatterns": [
      "/node_modules/"
    ]
  },
  "author": "Adekunle, Tobiloba, Peter",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "body-parser": "^1.20.1",
    "cookie-parser": "^1.4.6",
    "crypto": "^1.0.1",
    "googleapis": "^3.1.0",
    "jest": "^29.2.2",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.7.0",
    "path": "^0.12.7",
    "request": "^2.88.2",
    "sequelize-cli": "^6.5.2",
    "supertest": "^6.3.1",
    "util": "^0.12.5",
    "validator": "^13.7.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
